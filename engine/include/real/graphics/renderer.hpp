#ifndef REALLIB_RENDERER_HPP
#define REALLIB_RENDERER_HPP

namespace real {

/**
 * @brief its an abstract class that defines different methods for a renderer
 * so like a 2d renderer would have special methods and whatnot, but the backend would be optimized for 2d
 */
class Renderer {

};

}

#endif