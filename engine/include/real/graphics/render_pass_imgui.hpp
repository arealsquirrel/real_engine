#ifndef REALLIB_RENDER_PASS_IMGUI_HPP
#define REALLIB_RENDER_PASS_IMGUI_HPP

#include <real/graphics/render_pass.hpp>

namespace real {

/*
class RenderPassImGui : public RenderPass {
public:
    RenderPassImGui(Instance *_instance);
    ~RenderPassImGui();

    void draw(FrameContext context) override;
    void bind(FrameContext context) override;
};
*/

};

#endif