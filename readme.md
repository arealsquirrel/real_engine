# whats going on with the design

We are looking for speed. In development and runtime. We are taking a vulkan type aproach, a highlevel, monolithic instance class that we then have instaliation structs.
